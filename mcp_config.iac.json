{
  "description": "MCP servers for Terragrunt/OpenTofu infrastructure-as-code projects (memores-infrastructure). Includes HCL validation, state inspection, AWS resource queries, documentation indexing, and graph-based code intelligence.",
  "mcp_servers": {
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "{{REPO_PATH}}"
      ],
      "enabled": true,
      "description": "Read, search, and list files. Free. Requires Node.js/npx."
    },
    "git": {
      "type": "builtin",
      "module": "core.mcp_servers.GitServer",
      "enabled": true,
      "description": "Built-in. Git log, diff, blame, status, branch, show. Zero dependencies."
    },
    "memory": {
      "type": "builtin",
      "module": "core.mcp_servers.MemoryServer",
      "enabled": true,
      "description": "Built-in. Persistent knowledge graph: remember, recall, forget. Zero dependencies."
    },
    "thinking": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-sequential-thinking"
      ],
      "enabled": true,
      "description": "Sequential thinking for complex multi-step reasoning. Free. Requires Node.js/npx."
    },
    "lsp": {
      "type": "builtin",
      "module": "core.mcp_servers.LspServer",
      "enabled": true,
      "description": "Built-in. Python code intelligence: find definitions, references, list symbols, get code context."
    },
    "headroom": {
      "type": "stdio",
      "command": "headroom",
      "args": [
        "mcp",
        "serve",
        "--proxy-url",
        "http://127.0.0.1:8787"
      ],
      "enabled": true,
      "description": "Context compression via Headroom. Tools: headroom_compress, headroom_retrieve, headroom_stats. Uses running proxy at port 8787."
    },
    "terraform-registry": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@hashicorp/terraform-mcp-server"
      ],
      "enabled": true,
      "description": "Official HashiCorp Terraform MCP server. Interacts with the Terraform Registry API to search modules, providers, and fetch argument details for accurate schema-aware code generation."
    },
    "terragrunt-docs": {
      "type": "stdio",
      "command": "deno",
      "args": [
        "run",
        "-A",
        "jsr:@excoriate/mcp-terragrunt-docs@0.1.0"
      ],
      "env": {
        "GITHUB_TOKEN": ""
      },
      "enabled": true,
      "description": "Real-time Terragrunt document context provider and repository issue tracker via Deno."
    },
    "gortex": {
      "type": "stdio",
      "command": "gortex",
      "args": ["mcp", "serve"],
      "enabled": true,
      "description": "Code intelligence engine. Graph-based lookups, blast radius, clone detection, semantic search, cross-repo contracts. 257 languages. Replaces codebase-memory. Install: curl -fsSL https://get.gortex.dev | sh"
    }
  }
}
